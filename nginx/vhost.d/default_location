# V2 API Routing – /api/v2/ wird an calserver-api-v2 weitergeleitet
location /api/v2/ {
    proxy_pass         http://calserver-api-v2:8000/api/v2/;
    proxy_http_version 1.1;
    proxy_set_header   Host              $host;
    proxy_set_header   X-Real-IP         $remote_addr;
    proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
    proxy_read_timeout 120s;
}

# V2 Frontend Routing – /v2/ wird an calserver-frontend weitergeleitet
location /v2/ {
    proxy_pass         http://calserver-frontend:3000/;
    proxy_http_version 1.1;
    proxy_set_header   Host              $host;
    proxy_set_header   X-Real-IP         $remote_addr;
    proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
    # WebSocket Support (für Hot-Reload / Subscriptions)
    proxy_set_header   Upgrade           $http_upgrade;
    proxy_set_header   Connection        "upgrade";
}
